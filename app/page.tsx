"use client";
import { useEffect, useMemo, useRef, useState } from "react";
import * as XLSX from "xlsx";
import QRCode from "qrcode";
import { PDFDocument, StandardFonts, rgb } from "pdf-lib";

type Row = Record<string, string | number | boolean | null>;
const mm=(n:number)=>n*2.834645669;

export default function Home(){
 const [rows,setRows]=useState<Row[]>([]),[headers,setHeaders]=useState<string[]>([]),[qrCol,setQrCol]=useState(""),[labelW,setLabelW]=useState(70),[labelH,setLabelH]=useState(55),[cols,setCols]=useState(2),[showName,setShowName]=useState(true),[nameCol,setNameCol]=useState(""),[showLocation,setShowLocation]=useState(true),[locationCol,setLocationCol]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[previewQr,setPreviewQr]=useState(""),[dragging,setDragging]=useState(false);
 const fileInput=useRef<HTMLInputElement>(null);
 const preview=useMemo(()=>rows.slice(0,6),[rows]),firstPreview=preview[0];

 useEffect(()=>{let cancelled=false;if(!firstPreview||!qrCol){setPreviewQr("");return;}const value=String(firstPreview[qrCol]??"");if(!value){setPreviewQr("");return;}QRCode.toDataURL(value,{width:500,margin:1,errorCorrectionLevel:"M"}).then(url=>{if(!cancelled)setPreviewQr(url)}).catch(()=>{if(!cancelled)setPreviewQr("")});return()=>{cancelled=true}},[firstPreview,qrCol]);

 async function load(file:File){
   const valid=/\.(xlsx|xls|csv)$/i.test(file.name);
   if(!valid){setMessage("Bitte eine Excel- oder CSV-Datei auswählen.");return;}
   try{const data=await file.arrayBuffer();const wb=XLSX.read(data,{type:"array"});const sheet=wb.Sheets[wb.SheetNames[0]];const json=XLSX.utils.sheet_to_json<Row>(sheet,{defval:""});const hs=json.length?Object.keys(json[0]):[];setRows(json);setHeaders(hs);setQrCol(hs[0]||"");setNameCol(hs[1]||"");setLocationCol(hs[2]||"");setMessage(json.length+" Datensätze geladen: "+file.name)}catch{setMessage("Excel-Datei konnte nicht gelesen werden.")}}
 function onDrop(e:React.DragEvent<HTMLLabelElement>){e.preventDefault();e.stopPropagation();setDragging(false);const file=e.dataTransfer.files?.[0];if(file)load(file)}
 function onDragOver(e:React.DragEvent<HTMLLabelElement>){e.preventDefault();e.stopPropagation();e.dataTransfer.dropEffect="copy";setDragging(true)}
 function onDragLeave(e:React.DragEvent<HTMLLabelElement>){e.preventDefault();e.stopPropagation();setDragging(false)}

 async function makePdf(){if(!rows.length||!qrCol){setMessage("Bitte Excel-Datei und QR-Spalte auswählen.");return}if(labelW*cols+4*(cols-1)>190){setMessage("Die Etiketten sind zu breit für A4. Bitte Breite oder Anzahl pro Zeile reduzieren.");return}setBusy(true);setMessage("PDF wird erstellt …");try{const pdf=await PDFDocument.create();const font=await pdf.embedFont(StandardFonts.Helvetica);const perRow=Math.max(1,cols),pageW=mm(210),pageH=mm(297),margin=mm(10),gap=mm(4);const rowsPerPage=Math.max(1,Math.floor((pageH-margin*2+gap)/(mm(labelH)+gap))),maxPerPage=perRow*rowsPerPage;for(let start=0;start<rows.length;start+=maxPerPage){const page=pdf.addPage([pageW,pageH]);for(let i=0;i<Math.min(maxPerPage,rows.length-start);i++){const row=rows[start+i],c=i%perRow,r=Math.floor(i/perRow),x=margin+c*(mm(labelW)+gap),y=pageH-margin-(r+1)*mm(labelH)-r*gap,value=String(row[qrCol]??"");if(!value)continue;const png=await QRCode.toDataURL(value,{width:500,margin:1,errorCorrectionLevel:"M"}),img=await pdf.embedPng(png),qr=mm(Math.min(labelW-10,labelH-22));page.drawRectangle({x,y,width:mm(labelW),height:mm(labelH),borderColor:rgb(.78,.84,.87),borderWidth:.7});const qrX=x+(mm(labelW)-qr)/2,qrY=y+mm(labelH)-qr-mm(4);page.drawImage(img,{x:qrX,y:qrY,width:qr,height:qr});let ty=qrY-mm(5);page.drawText(value.slice(0,35),{x:x+mm(3),y:ty,size:9,font,maxWidth:mm(labelW)-mm(6)});ty-=mm(6);if(showName&&nameCol){page.drawText(String(row[nameCol]??"").slice(0,35),{x:x+mm(3),y:ty,size:7.5,font,maxWidth:mm(labelW)-mm(6)});ty-=mm(5)}if(showLocation&&locationCol)page.drawText(String(row[locationCol]??"").slice(0,35),{x:x+mm(3),y:ty,size:7.5,font,maxWidth:mm(labelW)-mm(6)})}}const bytes=await pdf.save(),safeBytes=new Uint8Array(bytes),blob=new Blob([safeBytes.buffer],{type:"application/pdf"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download="QR-Generator47-Etiketten.pdf";a.click();URL.revokeObjectURL(url);setMessage("PDF fertig – "+rows.length+" QR-Etiketten.")}catch(e){console.error(e);setMessage("PDF-Erstellung fehlgeschlagen.")}finally{setBusy(false)}}

 return <main className="min-h-screen"><header className="border-b bg-white"><div className="mx-auto max-w-6xl px-6 py-6"><div className="text-2xl font-bold">QR Generator<span className="text-sky-600">47</span></div><div className="text-sm text-slate-500">Excel → QR-Codes → druckfertiges PDF</div></div></header><div className="mx-auto grid max-w-6xl gap-6 px-6 py-8 lg:grid-cols-[1fr_400px]"><section className="space-y-6"><div className="card p-7"><h1 className="text-2xl font-bold">Excel-Datei importieren</h1><p className="mt-1 text-slate-500">Jede Zeile wird zu einem QR-Code-Etikett.</p><label onDrop={onDrop} onDragOver={onDragOver} onDragEnter={onDragOver} onDragLeave={onDragLeave} onClick={()=>fileInput.current?.click()} className={"mt-6 flex cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed px-6 py-12 text-center transition "+(dragging?"border-sky-500 bg-sky-100 scale-[1.01]":"border-sky-200 bg-sky-50 hover:bg-sky-100")}><div className="text-4xl">{dragging?"📥":"📊"}</div><div className="mt-3 font-semibold">{dragging?"Datei hier loslassen":"Excel hier ablegen oder Datei auswählen"}</div><div className="mt-1 text-sm text-slate-500">.xlsx / .xls / .csv</div><input ref={fileInput} type="file" accept=".xlsx,.xls,.csv" className="hidden" onChange={e=>e.target.files?.[0]&&load(e.target.files[0])}/></label>{message&&<div className="mt-4 rounded-xl bg-slate-50 px-4 py-3 text-sm">{message}</div>}</div>{rows.length>0&&<div className="card p-7"><h2 className="text-xl font-bold">Daten & Etikett</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm font-semibold">QR-Code-Inhalt<select value={qrCol} onChange={e=>setQrCol(e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal">{headers.map(h=><option key={h}>{h}</option>)}</select></label><label className="text-sm font-semibold">Etiketten pro Zeile<select value={cols} onChange={e=>setCols(+e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal"><option value={1}>1</option><option value={2}>2</option><option value={3}>3</option></select></label><label className="text-sm font-semibold">Breite (mm)<input type="number" min="20" value={labelW} onChange={e=>setLabelW(+e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal"/></label><label className="text-sm font-semibold">Höhe (mm)<input type="number" min="25" value={labelH} onChange={e=>setLabelH(+e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal"/></label><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={showName} onChange={e=>setShowName(e.target.checked)}/> Name/Bezeichnung anzeigen</label><label className="text-sm font-semibold">Name-Spalte<select value={nameCol} onChange={e=>setNameCol(e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal">{headers.map(h=><option key={h}>{h}</option>)}</select></label><label className="flex items-center gap-3 text-sm font-semibold"><input type="checkbox" checked={showLocation} onChange={e=>setShowLocation(e.target.checked)}/> Standort anzeigen</label><label className="text-sm font-semibold">Standort-Spalte<select value={locationCol} onChange={e=>setLocationCol(e.target.value)} className="mt-2 w-full rounded-xl border p-3 font-normal">{headers.map(h=><option key={h}>{h}</option>)}</select></label></div><button disabled={busy} onClick={makePdf} className="mt-7 w-full rounded-xl bg-sky-600 px-5 py-4 font-bold text-white hover:bg-sky-700 disabled:opacity-50">{busy?"PDF wird erstellt …":"📄 Druckfertiges PDF erstellen"}</button></div>}</section><aside className="card h-fit p-6 lg:sticky lg:top-6"><div className="flex items-center justify-between"><div><h2 className="text-xl font-bold">Druckvorschau</h2><p className="text-sm text-slate-500">QR oben, Text darunter</p></div><span className="rounded-full bg-sky-50 px-3 py-1 text-xs font-semibold text-sky-700">A4</span></div><div className="mt-5 rounded-2xl bg-slate-100 p-5"><div className="mx-auto overflow-hidden rounded-lg border bg-white p-3 shadow-sm" style={{width:"min(100%, 300px)",aspectRatio:labelW+"/"+labelH}}>{firstPreview&&qrCol?<div className="flex h-full flex-col items-center justify-center text-center">{previewQr?<img src={previewQr} alt="QR Vorschau" className="h-auto max-h-[62%] max-w-[72%] object-contain"/>:<div className="text-sm text-slate-400">QR wird geladen …</div>}<div className="mt-2 w-full break-words text-sm font-bold">{String(firstPreview[qrCol]??"")}</div>{showName&&nameCol&&<div className="w-full truncate text-xs text-slate-600">{String(firstPreview[nameCol]??"")}</div>}{showLocation&&locationCol&&<div className="w-full truncate text-xs text-slate-500">{String(firstPreview[locationCol]??"")}</div>}</div>:<div className="flex h-full items-center justify-center text-sm text-slate-400">Excel-Datei hochladen</div>}</div></div>{preview.length>0&&<div className="mt-5"><div className="mb-2 text-sm font-semibold">Weitere Datensätze</div><div className="space-y-2">{preview.slice(1).map((row,i)=><div key={i} className="rounded-xl border p-3 text-sm"><div className="font-bold">{String(row[qrCol]??"")}</div>{showName&&nameCol&&<div className="text-slate-600">{String(row[nameCol]??"")}</div>}{showLocation&&locationCol&&<div className="text-slate-500">{String(row[locationCol]??"")}</div>}</div>)}</div></div>}<div className="mt-6 rounded-xl bg-slate-50 p-4 text-xs text-slate-500">Die Excel-Daten werden vollständig im Browser verarbeitet. Es ist kein Backend und keine Datenbank nötig.</div></aside></div></main>
}